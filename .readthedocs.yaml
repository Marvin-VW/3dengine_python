version: 2

build:
  tools:
    python: "3.12"

python:
  version: "3.12"
  install:
    - method: pip
      path: .
    - requirements: requirements.txt

sphinx:
  configuration: docs/conf.py

build:
  commands:
    - tox -e docs
